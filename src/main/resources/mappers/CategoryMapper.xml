<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.imall.dao.CategoryMapper">
    <resultMap id="CategoryResultMap" type="com.imall.pojo.Category">
        <constructor>
            <idArg column="id" javaType="java.lang.Integer" jdbcType="INTEGER"/>
            <arg column="parent_id" javaType="java.lang.Integer" jdbcType="INTEGER"/>
            <arg column="name" javaType="java.lang.String" jdbcType="VARCHAR"/>
            <arg column="status" javaType="java.lang.Boolean" jdbcType="BIT"/>
            <arg column="sort_order" javaType="java.lang.Integer" jdbcType="INTEGER"/>
            <arg column="create_time" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
            <arg column="update_time" javaType="java.util.Date" jdbcType="TIMESTAMP"/>
        </constructor>
    </resultMap>
    <sql id="Category_Column_List">
    id, parent_id, name, status, sort_order, create_time, update_time
  </sql>
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="CategoryResultMap">
        select
        <include refid="Category_Column_List"/>
        from mmall_category
        where id = #{id,jdbcType=INTEGER}
    </select>
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from mmall_category
    where id = #{id,jdbcType=INTEGER}
  </delete>
    <insert id="insert" parameterType="com.imall.pojo.Category">
    insert into mmall_category (id, parent_id, name, 
      status, sort_order, create_time, 
      update_time)
    values (#{id,jdbcType=INTEGER}, #{parentId,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR}, 
      #{status,jdbcType=BIT}, #{sortOrder,jdbcType=INTEGER}, now(),
      now())
  </insert>
    <insert id="insertSelective" parameterType="com.imall.pojo.Category">
        insert into mmall_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="parentId != null">
                parent_id,
            </if>
            <if test="name != null">
                name,
            </if>
            <if test="status != null">
                status,
            </if>
            <if test="sortOrder != null">
                sort_order,
            </if>
            create_time,
            update_time,
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="parentId != null">
                #{parentId,jdbcType=INTEGER},
            </if>
            <if test="name != null">
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="status != null">
                #{status,jdbcType=BIT},
            </if>
            <if test="sortOrder != null">
                #{sortOrder,jdbcType=INTEGER},
            </if>
            now(),
            now(),
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.imall.pojo.Category">
        update mmall_category
        <set>
            <if test="parentId != null">
                parent_id = #{parentId,jdbcType=INTEGER},
            </if>
            <if test="name != null">
                name = #{name,jdbcType=VARCHAR},
            </if>
            <if test="status != null">
                status = #{status,jdbcType=BIT},
            </if>
            <if test="sortOrder != null">
                sort_order = #{sortOrder,jdbcType=INTEGER},
            </if>
            <if test="createTime != null">
                create_time = #{createTime,jdbcType=TIMESTAMP},
            </if>
            update_time = now(),
        </set>
        where id = #{id,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="com.imall.pojo.Category">
    update mmall_category
    set parent_id = #{parentId,jdbcType=INTEGER},
      name = #{name,jdbcType=VARCHAR},
      status = #{status,jdbcType=BIT},
      sort_order = #{sortOrder,jdbcType=INTEGER},
      create_time = #{createTime,jdbcType=TIMESTAMP},
      update_time = now()
    where id = #{id,jdbcType=INTEGER}
  </update>

    <select id="selectCountByKeySelective" parameterType="com.imall.pojo.Category" resultType="int">
        SELECT
        count(1)
        from mmall_category category

        <trim prefix="where" prefixOverrides="and|or">
            <if test="id!=null">
                and category.id=#{id}
            </if>
            <if test="parentId!=null">
                and category.parent_id=#{parentId}
            </if>
            <if test="name!=null">
                and category.name=#{name}
            </if>
            <if test="status!=null">
                and category.status=#{status}
            </if>
            <if test="sortOrder!=null">
                and category.sort_order=#{sortOrder}
            </if>
            <if test="createTime">
                and category.create_time=#{createTime}
            </if>
            <if test="updateTime">
                and category.update_time=#{updateTime}
            </if>
        </trim>
    </select>

    <select id="selectSubCategoryByParentId" resultMap="CategoryResultMap" parameterType="int">

    </select>

    <select id="selectByKeySelective" resultMap="CategoryResultMap" parameterType="com.imall.pojo.Category">
        SELECT
        <include refid="Category_Column_List"/>
        from mmall_category category

        <trim prefix="where" prefixOverrides="and|or">
        <if test="id!=null">
            and category.id=#{id}
        </if>
        <if test="parentId!=null">
            and category.parent_id=#{parentId}
        </if>
        <if test="name!=null">
            and category.name like CONCAT(CONCAT('%',#{name}),'%')
        </if>
        <if test="status!=null">
            and category.status=#{status}
        </if>
        <if test="sortOrder!=null">
            and category.sort_order=#{sortOrder}
        </if>
        <if test="createTime">
            and category.create_time=#{createTime}
        </if>
        <if test="updateTime">
            and category.update_time=#{updateTime}
        </if>
        </trim>
    </select>
</mapper>